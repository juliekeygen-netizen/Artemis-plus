name: One-shot Keep Alive POC validation

on:
  push:
    branches:
      - feature/background-keep-alive-poc
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-verify-push:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/background-keep-alive-poc
          fetch-depth: 0
          submodules: recursive

      - name: Apply Keep Alive POC patches
        run: |
          python3 .github/keep_alive_patch.py
          python3 .github/keep_alive_audit_patch.py
          python3 .github/keep_alive_audit2_patch.py
          python3 .github/keep_alive_audit3_patch.py

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK and NDK
        run: sdkmanager "platforms;android-36" "ndk;27.0.12077973"

      - name: Make Gradle executable
        run: chmod +x gradlew

      - name: Compile non-root debug variant
        run: ./gradlew :app:compileNonRoot_gameDebugJavaWithJavac --stacktrace

      - name: Run Keep Alive and lifecycle regressions
        run: >-
          ./gradlew :app:testNonRoot_gameDebugUnitTest
          --tests "com.limelight.preferences.BackgroundStreamingPolicyTest"
          --tests "com.limelight.preferences.StreamSettingsFragmentLifecycleTest"
          --tests "com.limelight.PipOverlayTransitionStateTest"
          --tests "com.limelight.PipAspectRatioHelperTest"
          --tests "com.limelight.BottomEdgeStartGestureDetectorTest"
          --stacktrace

      - name: Commit verified product changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main app/src/test
          if git diff --cached --quiet; then
            echo "No product changes to commit"
            exit 0
          fi
          git commit -m "feat: harden experimental Keep Connection Alive"
          git push origin feature/background-keep-alive-poc
