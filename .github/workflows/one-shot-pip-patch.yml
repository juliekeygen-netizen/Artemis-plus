name: One-shot Android 11 PiP source patch

on:
  push:
    branches:
      - fix/android11-pip-entry-guard

permissions:
  contents: write

jobs:
  patch:
    if: github.actor == 'juliekeygen-netizen'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v7
        with:
          ref: fix/android11-pip-entry-guard

      - name: Apply exact Game source edit
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          game_path = Path('app/src/main/java/com/limelight/Game.java')
          game = game_path.read_text(encoding='utf-8')
          old = """if (autoEnterPip && Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
                      enterPictureInPictureMode(getPictureInPictureParams(false));
                  }
                  return true;"""
          new = """if (autoEnterPip && Build.VERSION.SDK_INT < Build.VERSION_CODES.S) {
                      PipManualEntryGuard.tryEnter(() ->
                              enterPictureInPictureMode(getPictureInPictureParams(false)));
                  }
                  return true;"""
          count = game.count(old)
          if count != 1:
              raise SystemExit(f'Expected exactly one Android R PiP block, found {count}')
          game_path.write_text(game.replace(old, new, 1), encoding='utf-8')
          PY

      - name: Commit patched Game source
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git diff -- app/src/main/java/com/limelight/Game.java
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java/com/limelight/Game.java
          git commit -m 'fix: guard Android 11 manual PiP entry'
          git push origin HEAD:fix/android11-pip-entry-guard
